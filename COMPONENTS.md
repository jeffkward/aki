<!-- GENERATED by `bun run components:index` — do not hand-edit. -->
# aki components

13 components. Import from the barrel: `import { Button, Card } from "../components"`.

Every one of these is rendered at **/theme**. If you need something that is not
here, add it (see `.claude/skills/component/SKILL.md`) — do not inline styles
and do not invent a one-off.

| Component | What it's for | Props | File |
|---|---|---|---|
| `<Bubble>` | A chat message. `me` right-aligns and uses the accent token. | `from` | `app/components/bubble.tsx` |
| `<Button>` | The only button. Never style a <button> inline. | `variant`, `type`, `size`, `disabled`, `tip` | `app/components/button.tsx` |
| `<Callout>` | An inline notice. The emoji lives here, not in the CSS. | `kind` | `app/components/callout.tsx` |
| `<Card>` | The standard surface. Anything grouped goes in one of these. | — | `app/components/card.tsx` |
| `<Chip>` | A pill. Use for tags, filters, and small inline labels. | `active`, `onRemove` | `app/components/chip.tsx` |
| `<MetaChip>` | A chip that shows a label/value pair. | `label` | `app/components/chip.tsx` |
| `<ChipAdd>` | The dashed "add + Enter" pill. | `name`, `placeholder` | `app/components/chip.tsx` |
| `<Field>` | A labelled input. Use instead of a bare <input> so labels stay consistent. | `name`, `label`, `type`, `value`, `placeholder`, `required` | `app/components/field.tsx` |
| `<Select>` | A labelled dropdown. The chevron is markup, not a background-image, so its colour comes from a token like everything else. | `name`, `label`, `options`, `value` | `app/components/field.tsx` |
| `<Stat>` | A single number with a label. For dashboards and summary rows. | `n`, `label` | `app/components/stat.tsx` |
| `<Table>` | Wraps a table so wide content scrolls inside it, never the page. | `headers` | `app/components/table.tsx` |
| `<Timestamp>` | A compact relative label with the exact date on hover. | `value`, `prefix` | `app/components/timestamp.tsx` |
| `<Tip>` | Hover help on any text. Never write a bespoke hover handler. | `text` | `app/components/tip.tsx` |
